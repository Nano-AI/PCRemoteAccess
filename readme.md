Run `python -m flask run --host=0.0.0.0` to run on local network.

Run `ipconfig` to get IP and open `<pc's ip>:<port listed in flask>/` in a browser.

Username and password can be changed and read in `config.json`.